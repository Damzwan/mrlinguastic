overwrite: true
schema: "src/schema.graphql"
generates:
  src/gen-types.ts:
    documents: "src/queries/*.graphql"
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-vue-apollo"
